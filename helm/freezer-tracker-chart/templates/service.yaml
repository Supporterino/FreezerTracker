apiVersion: v1
kind: Service
metadata:
  name: {{ include "freezer-tracker-chart.fullname" . }}
  labels:
    {{- include "freezer-tracker-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "freezer-tracker-chart.selectorLabels" . | nindent 4 }}
