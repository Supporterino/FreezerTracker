apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "freezer-tracker-server.fullname" . }}
  labels:
    {{- include "freezer-tracker-server.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
